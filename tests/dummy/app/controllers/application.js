import Ember from 'ember';

export default Ember.Controller.extend({
    number: function() {
        return '';
    }.property(),
    expiration: function() {
        return '';
    }.property()
});
